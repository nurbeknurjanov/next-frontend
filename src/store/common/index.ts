import {
  breadcrumbs,
  title,
  pageTopContentButtons,
  login,
  hydrated,
  notify,
} from './slices';

export const common = {
  title,
  breadcrumbs,
  pageTopContentButtons,
  login,
  hydrated,
  notify,
};
