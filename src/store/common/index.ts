import {
  breadcrumbs,
  title,
  buttonsContent,
  hydrate,
  notify,
  auth,
  queryError,
} from './slices';

export const common = {
  title,
  breadcrumbs,
  buttonsContent,
  hydrate,
  notify,
  auth,
  queryError,
};
